#pragma once

// Standard Library headers for data containers
#include <unordered_map>  // For std::unordered_map
#include <unordered_set>  // For std::unordered_set
#include <vector>         // For std::vector

// Standard Library headers for string and stream operations
#include <string>         // For std::string
#include <sstream>        // For std::istringstream

// Standard Library headers for algorithms and character functions
#include <algorithm>      // For std::transform and other algorithms
#include <cctype>         // For character handling functions like std::tolower

// Standard Library header for I/O operations
#include <iostream>       // For std::cin, std::cout
